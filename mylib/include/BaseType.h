#pragma once
#include<vector>
#include <string>

namespace Library_Manager
{
	using information = std::string;
	using Word = information;
	using WordList = std::vector<Word>;
	using Command = std::vector<information>;
}